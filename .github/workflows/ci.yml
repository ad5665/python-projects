name: Python CI

on:
  push:
    branches: [ "**" ]
  pull_request:
    branches: [ "**" ]

jobs:
  file_organiser:
    uses: ./.github/workflows/tests.yml@file-org-workflows
    with:
      project-path: Beginner/file_organiser
